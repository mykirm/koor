# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# koor — local-only data + logs
# (gitignore does not support inline comments — keep patterns on their own lines)
# live demo log (may contain arbitrary user input)
/runs.jsonl
# raw mcPHASES (re-download from PhysioNet)
/data/raw/
# mcPHASES per-row provenance — participant linkage (PhysioNet Restricted DUA 1.5.0): local audit trail only, never committed
/data/mcphases_provenance.md
# locally-seeded corpus overlay (physiological tuples regenerated from raw mcPHASES) — never committed
/data/thoughts.seeded.json
# derived embedding cache — rebuild with scripts/build-embeddings.ts
/data/embeddings.*.json
*.local.json
# local README backup
*.bak

# Evaluation outputs ARE committed as evidence (NOT ignored):
#   data/eval-runs.jsonl, data/eval-scores.jsonl, data/scores.jsonl

PEER_REVIEW_AUDIT.md

# Local helpers — not part of the public repo
/COMMIT_PLAN.sh
/CONTINUE.sh
/FINISH.sh
/CLEANUP.sh

# Internal planning docs — kept locally, not pushed
docs/demo-video-script.md
docs/video-script.md
docs/submission-qa.md
docs/ux-design-koor-2026-06-02.md
docs/rater-guide.md

# Local helpers — not part of the public repo
/COMMIT_PLAN.sh
/CONTINUE.sh
/FINISH.sh
/CLEANUP.sh

# Internal planning docs — kept locally, not pushed
docs/demo-video-script.md
docs/video-script.md
docs/submission-qa.md
docs/ux-design-koor-2026-06-02.md
docs/rater-guide.md

# presentation/video working assets — local only
*.pptx
*.key
*.mov
*.mp4
